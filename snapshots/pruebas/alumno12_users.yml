version: 2

sources:

  - name: db # name of the source 
    schema: alumno12_users # this is the schema our raw data lives in
    database: alumno12_dev_bronze_db # this is the name of our database

    tables:

      - name: users
        columns:
          - name: Nombre
          - name: DNI
          - name: email
          - name: fecha_alta_sistema